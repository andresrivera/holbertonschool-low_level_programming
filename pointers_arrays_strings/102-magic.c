#include <stdio.h>

/**
* main - function
* Description: add line to code
*
* Return: Always 0
*/
int main(void)
{
int i, a[5], *p;
a[2] = 1024;
p = &n;
*(p + 5) = 98;
printf("a[2] = %d\n", a[2]);
return (s);
}
