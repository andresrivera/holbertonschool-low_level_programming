#include "main.h"
#include <ctype.h>

/**
* _islower(int c) - function
* description - Check for lowercase characters
*
* Return: 0
*/
int _islower(int c)
{
if (islower(c) != 0)
{
_putchar('0');
}
else
{
_putchar('1');
}
}
