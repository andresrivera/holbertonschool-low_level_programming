#include "main.h"

/**
* _isalpha - check is is a letter
* @c: is a char
* Description: Check if is a uppercase or lowercase letter
*
* Return: 1 is is a letter, 0 otherwise
*/
int _isalpha(int c)
{
if ((c >= 65 && c <= 90) || (c >= 97 && c <= 122))
{
return (1);
}
return (0);
}
