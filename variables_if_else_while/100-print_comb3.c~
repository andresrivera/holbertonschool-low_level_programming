#include <stdio.h>

/**
* main - main function
* description - print all combination of 2 digits to 100
*
*Return: Always 0
*/
int main(void)
{
int i, j;
for (i = 0; i <= 9; i++)
{
for (j = 0; j <= 9; j++)
{
if (i < j)
{
putchar(i + '0');
putchar(j + '0');
if(i != 9 && j != 9)
{
putchar(' ');
}
}
}
}
putchar('\n');
return (0);
}
