#include "main.h"

/**
* _sqrt_recursion - function
* @n: number
* Description: return natural square root
*
* Return: Always 0
*/
int _sqrt_recursion(int n)
{
  if (n < 0)
    return (-1);
      if (n == 1)
	return (1)
  
}
